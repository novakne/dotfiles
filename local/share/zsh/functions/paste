# Submit paste to <https://0x0.st/>
#
# USAGE: paste <file> or <cmd> | paste
# REQUIREMENTS: curl

emulate -L zsh
setopt extendedglob

local file=${1:-/dev/stdin}
curl -F'file=@${file}' https://0x0.st

# vim: ft=zsh
