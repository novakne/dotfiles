#!/usr/bin/env bash
#
# Fuzzy find bin
#
# USAGE: fmenu
# REQUIREMENTS: fzf bin-list

fmenu()
{
	local app
	app=$(bin-list | fzf -0 -1 -e -i --prompt='launch -> ')

	setsid "${app}" >/dev/null 2>&1 &
}

fmenu "$@"
