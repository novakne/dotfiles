#!/usr/bin/env bash
#
# use .desktop in dmenu-wl


declare -a dmenu_opts
dmenu_opts=(-i -h 28 -p 'App: ' -fn 'Overpass Nerd Font' -nb '#38383c' -nf '#d2ccd6' -sb '#e8ec77' -sf '#232227')

eval "$(dot-desktop "$(dot-desktop | dmenu-wl "${dmenu_opts[@]}")")"

exit 1
