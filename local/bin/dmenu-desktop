#!/usr/bin/env bash
#
# use .desktop in dmenu-wl


declare -a dmenu_opts

dmenu_opts=(
  -i -h 28
  -p 'App: '
  -fn 'Overpass'
  -nb '#23222b'
  -nf '#eae9ee'
  -sb '#e8ec77'
  -sf '#23222b'
)

eval "$(dot-desktop "$(dot-desktop | dmenu-wl "${dmenu_opts[@]}")")"

exit 1

unset dmenu_opts

